# real credentials never go in git
users.txt
*.csv
# playwright-core and its lockfile-installed deps
node_modules/
# the host manifest is generated per machine by host/install.js (absolute paths + your ext id)
host/com.umsreconciler.crmloadtest.json
